# Cochain 前端 P2-P6 实施与验收证据

> 2026-08-18 更新：评审定稿已将原 18 个分散业务入口收敛为 6 个业务入口，同时保留默认权限中心及其 5 个子页面。本文以下内容保留为历史实施记录，当前验收结论以 `docs/figma-reviewed-implementation-evidence.md` 为准。

> 日期：2026-08-11  
> 结论：P1 鉴权门禁通过后，P2-P6 已完成；18 个业务页面在真实 Keycloak/RBAC、Mock 业务数据模式下可访问。P7 真实业务 API 绑定尚未开始。

## 完成范围

-   P2：Cochain 项目标识、环境配置、语义 Token、经典后台壳层与响应式抽屉。
-   P3：18 类资源的 TypeScript 契约、Mock/HTTP 双适配器、通用数据页、状态与权限测试辅助。
-   P4：分包批次、批次零件、工作包、工作包零件、供应商推荐 5 个页面。
-   P5：供应商、绩效、排名快照、供应商品类、轮流游标、历史供应商 6 个页面。
-   P6：三级品类、特殊品类、左右件规则、左右件维护、容量配置、业务日志、系统日志 7 个页面。
-   未定义真实端点的导入/导出仅显示“待绑定”策略，不猜测 URL，也不新增无效 RBAC 规则。
-   迁移清理：旧业务页面、工作流/待办/BPMN、前台会员端、旧布局、旧编辑器与资产已删除；动态组件仅允许 Cochain 与权限中心目录。

## 设计技能落实

-   `frontend-design`：以采购协同任务效率为核心建立明确的信息层级和五阶段分包流程。
-   `design-taste-frontend`：用于复制体审计、反模板化检查与交付前一致性检查；未套用营销页结构。
-   `ui-ux-pro-max`：完成 enterprise procurement/admin 与 Vue 检索；与 `DESIGN.md` 冲突的琥珀 CTA、公网字体建议未采用。
-   最终界面保持单一 Action Blue、系统字体、无装饰渐变、无卡片阴影滥用、44px 目标、1024px 折叠与 768px 抽屉。

## 自动检查

| 检查                           | 结果                                                                                    |
| ------------------------------ | --------------------------------------------------------------------------------------- |
| SQL 动态组件路径与本地文件对应 | 18/18，缺失 0                                                                           |
| 源码旧项目标识扫描             | 通过；`flow/flow-web/oversia/BuildAdmin`、旧 Dashboard、旧作者路径与工作流 Token 命中 0 |
| 全量 `npm run lint`            | 通过，0 error、0 warning                                                                |
| 全量 `npm run typecheck`       | 通过，0 error                                                                           |
| `npm run build`                | 通过，Vite 1699 modules transformed                                                     |

清理前生产构建为 5410 modules，并包含 `qrcode-svg`、`onnxruntime-web` 与巨型编辑器 chunk 提醒；清理后这些依赖和提醒均已消失。权限组表单 chunk 已从约 8 MB 降至约 9.6 kB。

## 浏览器验收

-   使用 Keycloak 标准交互式登录，未依赖 Direct Access Grant。
-   RBAC 返回 4 个业务目录、18 个业务页面及现有权限中心菜单。
-   18 个动态路由逐一加载，标题全部匹配；深链刷新保持当前页面。
-   桌面端标题与菜单正常；移动端菜单触发器可见，抽屉宽度 280px，无页面级横向溢出。
-   浏览器控制台错误 0，请求失败 0。
-   无效/缺失 Token 返回 401，缺失/错误项目返回 403；当前账号为 project super，未声称完成非 super 角色裁剪验收。

## 仍待处理

1. P7：需要真实业务 API base URL、网络/CORS/代理方案、上传限制、下载命名与错误码样例，随后逐资源切换 HTTP Adapter。
2. T004：当前目录不是 Git 工作树；新 Cochain 仓库、远程与目标分支仍未定义。源复制基线可追溯，但不能替代正式版本控制门禁。
3. P8：非 super 角色裁剪、真实 API 双模式回归、批准环境部署与回滚演练尚未完成。
